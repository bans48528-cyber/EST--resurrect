# 已验证固件发布包

| 版本 | 状态 | 用途 |
| --- | --- | --- |
| `M0.16A` | 已实机验证 | 使用官方 MSP 的长入口信标版本，适合启动链路诊断 |
| `M0.17A` | 已实机验证 | 约 2 秒入口签名，之后仅蓝灯慢闪；M0.18A 的升级来源版本 |
| `M0.18A` | 已实机验证 | 约 2 秒入口签名，之后仅蓝灯慢闪；M0.19A 的升级来源版本 |
| `M0.19A` | 已实机验证 | 屏幕显示更新 `0-100`，随后约 2 秒入口签名，之后仅蓝灯慢闪；M0.20A 的升级来源版本 |
| `M0.20A` | 已实机验证 | 使用模块化发送器完成 260 包升级；M0.21A 的升级来源版本 |
| `M0.21A` | 已实机验证 | 使用带 manifest 校验、版本防错和日志的工程化升级工具完成升级 |
| `M0.22A` | 已实机验证 | 电源、LED、时基和看门狗拆分为独立模块 |
| `M0.23A` | 已实机验证 | 六个面板按键及组合按键诊断通过 |
| `M0.25A` | 已实机验证 | 屏幕显示版本号、USB 心跳和按键诊断通过 |
| `M0.32A` | 已实机验证 | 由 M0.31A 自升级，屏幕显示版本号、蓝灯闪烁和 USB 心跳均正常；新的 GCC 基线版本 |
| `M0.33A` | 已实机验证 | A 口马达在命令触发后完成低速正转、暂停、反转和停止；上电不自动转 |
| `M0.34A` | 已实机验证 | A 口测速反馈通过：正转 `+165`、反转 `-168`，方向相反且计数接近 |
| `M0.35A` | 已实机验证 | A 口支持 1%-100% 可调测试；100% 实测正转 `+421`、反转 `-375`，方向正确并最终关闭驱动 |
| `M0.36A` | 已实机验证 | A 口 60% 同条件停车对比：自由滑行后续 `296` 脉冲，主动刹车后续 `39` 脉冲；两种停止方式及其硬件电平均已确认 |
| `M0.37A` | 已实机验证 | B 口 30% 正反转测速 `+163/-168`，确认 A/B 端口隔离；B 口自由滑行/主动刹车为 `323/37` 个后续脉冲 |
| `M0.38A` | 已实机验证 | A、B 两个大型马达同时正转、刹车、反转和停止；测速为 A `+162/-172`、B `+178/-178` |
| `M0.39A` | 已实机验证 | C 口中型马达完成正转、反转、测速和停止；同一马达在 C 与已验证 A 口都表现出类似的正反向计数不对称，因此排除 C 口接线故障 |
| `M0.40A` | 已实机验证 | M0.39A 正常开机时直接接收升级包并在最终 ACK 后自动关机；旧引导搬运后成功启动。D 口中型马达连续两次完成正反转、测速和停止 |
| `M0.43A` | 已实机验证 | 新增通用马达命令 `0x17`：A-D 端口选择、`-100%..+100%` 有符号功率、自由滑行、主动刹车、测速读取和计数清零。A/D 独立与同时控制通过；D 口 80% 自由滑行/主动刹车后续计数为 `136/13` |
| `M0.44A` | 已实机验证 | 1 号输入口识别为 `0x1D` EST/EV3 颜色传感器；57600 波特率持续通信正常，反射灰度实测 `65`，环境光和颜色模式均可切换，旧升级协议和马达命令保持不变 |
| `M0.45A` | 已实机验证 | LCD 实时显示反射灰度、环境光数字或颜色名称，任意按键可循环切换三种模式；颜色识别精度受传感器内部固定判断规则限制 |
| `M0.46A` | 已实机验证 | 1-4 号输入口各用独立串口同时识别 EST/EV3 UART 传感器；LCD 四行同时显示连接状态或实时读数。四个输入口均由同一只颜色/灰度传感器逐口确认正常，2 号口 USB 实测灰度值 `55` |
| `M0.47A` | 已实机验证 | EST/EV3 被动触碰传感器可自动识别，LCD 实时显示 `UP`（松开）与 `DOWN`（按下）；按下、松开、按键防抖及四个输入口均由实物确认正常，颜色/灰度 UART 传感器逻辑保持不变 |
| `M0.48A` | 已实机验证 | 支持类型 `0x1E` 的 EST/EV3 超声波传感器；实物确认自动识别、LCD 实时距离显示及物体移动时数值变化正常，旧传感器和升级协议保持不变 |
| `M0.49A` | 实机未通过 | 首版温度传感器协议实现；2 号口的供电及接入电气变化可见，但模块未完成识别。查明其 I²C 节奏快于旧固件，保留此包仅用于问题追溯 |
| `M0.50A` | 实机未通过 | 温度传感器通信已降至旧固件使用的约 50 微秒半周期，但实物仍未完成识别；进一步核对发现尚未复刻旧固件的 I²C 输入通路切换 |
| `M0.51A` | 已实机验证 | 按旧固件的 pin 1/pin 6 电压规则判断 I²C 模块，识别前将 `RXIN_EN` 切到高电平并等待通路稳定；实物确认温度传感器能够自动识别并正常显示温度 |
| `M0.52A` | 已实机验证 | 新增 USB 应用协议 `1.0` 的只读整机快照 `0x19` 和 V5 `PF3` 电池采样；一次返回版本、电量、按键、四个马达及四个输入口状态。修复电脑端超过 64 字节回复的截断后，实机完整读取成功 |
| `M0.53A` | 待实机验证 | LCD 同时显示四路输入和 A-D 马达累计计数；开机自动执行背光全亮、变暗、熄灭、恢复全亮，并通过板载 VS 音频芯片播放两段短测试音 |
| `M0.54A` | 部分实机验证 | LCD 电量显示 `BAT:100%`，背光全亮、变暗、熄灭、恢复全亮均通过；板载 VS 内部测试音可听见且显示 `SND:DONE`，但音量很小，未达到“正常播放”验收标准 |
| `M0.55A` | 部分实机验证 | 改用 WAV 数据通道后实物确认声音明显增大，但实际播放时间很短，没有达到“正常播放一次”的验收标准 |
| `M0.56A` | 实机未通过 | 将 WAV 测试数据延长到约 5 秒后，实物确认播放过程仍不正常。音频问题暂缓，后续需单独核对 VS 数据流结束/填充、时钟和功放控制；本包只用于问题追溯 |

目录内只保存可交给升级工具的 `.upgrade.bin` 和对应 manifest。不要发送 `M0.09A`、存在 LCD/USB 引脚冲突的 `M0.24A`，也不要发送构建目录中的原始 `.bin`。
