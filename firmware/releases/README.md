# 已验证固件发布包

| 版本 | 状态 | 用途 |
| --- | --- | --- |
| `M0.16A` | 已实机验证 | 使用官方 MSP 的长入口信标版本，适合启动链路诊断 |
| `M0.17A` | 已实机验证 | 约 2 秒入口签名，之后仅蓝灯慢闪；M0.18A 的升级来源版本 |
| `M0.18A` | 已实机验证 | 约 2 秒入口签名，之后仅蓝灯慢闪；M0.19A 的升级来源版本 |
| `M0.19A` | 已实机验证 | 屏幕显示更新 `0-100`，随后约 2 秒入口签名，之后仅蓝灯慢闪；M0.20A 的升级来源版本 |
| `M0.20A` | 已实机验证 | 使用模块化发送器完成 260 包升级；M0.21A 的升级来源版本 |
| `M0.21A` | 已实机验证 | 使用带 manifest 校验、版本防错和日志的工程化升级工具完成升级 |
| `M0.22A` | 已实机验证 | 电源、LED、时基和看门狗拆分为独立模块 |
| `M0.23A` | 已实机验证 | 六个面板按键及组合按键诊断通过 |
| `M0.25A` | 已实机验证 | 屏幕显示版本号、USB 心跳和按键诊断通过 |
| `M0.32A` | 已实机验证 | 由 M0.31A 自升级，屏幕显示版本号、蓝灯闪烁和 USB 心跳均正常；新的 GCC 基线版本 |
| `M0.33A` | 已实机验证 | A 口马达在命令触发后完成低速正转、暂停、反转和停止；上电不自动转 |
| `M0.34A` | 已实机验证 | A 口测速反馈通过：正转 `+165`、反转 `-168`，方向相反且计数接近 |
| `M0.35A` | 已实机验证 | A 口支持 1%-100% 可调测试；100% 实测正转 `+421`、反转 `-375`，方向正确并最终关闭驱动 |
| `M0.36A` | 已实机验证 | A 口 60% 同条件停车对比：自由滑行后续 `296` 脉冲，主动刹车后续 `39` 脉冲；两种停止方式及其硬件电平均已确认 |
| `M0.37A` | 已实机验证 | B 口 30% 正反转测速 `+163/-168`，确认 A/B 端口隔离；B 口自由滑行/主动刹车为 `323/37` 个后续脉冲 |
| `M0.38A` | 已实机验证 | A、B 两个大型马达同时正转、刹车、反转和停止；测速为 A `+162/-172`、B `+178/-178` |
| `M0.39A` | 已实机验证 | C 口中型马达完成正转、反转、测速和停止；同一马达在 C 与已验证 A 口都表现出类似的正反向计数不对称，因此排除 C 口接线故障 |
| `M0.40A` | 已实机验证 | M0.39A 正常开机时直接接收升级包并在最终 ACK 后自动关机；旧引导搬运后成功启动。D 口中型马达连续两次完成正反转、测速和停止 |
| `M0.43A` | 已实机验证 | 新增通用马达命令 `0x17`：A-D 端口选择、`-100%..+100%` 有符号功率、自由滑行、主动刹车、测速读取和计数清零。A/D 独立与同时控制通过；D 口 80% 自由滑行/主动刹车后续计数为 `136/13` |
| `M0.44A` | 已实机验证 | 1 号输入口识别为 `0x1D` EST/EV3 颜色传感器；57600 波特率持续通信正常，反射灰度实测 `65`，环境光和颜色模式均可切换，旧升级协议和马达命令保持不变 |
| `M0.45A` | 已实机验证 | LCD 实时显示反射灰度、环境光数字或颜色名称，任意按键可循环切换三种模式；颜色识别精度受传感器内部固定判断规则限制 |
| `M0.46A` | 已实机验证 | 1-4 号输入口各用独立串口同时识别 EST/EV3 UART 传感器；LCD 四行同时显示连接状态或实时读数。四个输入口均由同一只颜色/灰度传感器逐口确认正常，2 号口 USB 实测灰度值 `55` |
| `M0.47A` | 已实机验证 | EST/EV3 被动触碰传感器可自动识别，LCD 实时显示 `UP`（松开）与 `DOWN`（按下）；按下、松开、按键防抖及四个输入口均由实物确认正常，颜色/灰度 UART 传感器逻辑保持不变 |
| `M0.48A` | 已实机验证 | 支持类型 `0x1E` 的 EST/EV3 超声波传感器；实物确认自动识别、LCD 实时距离显示及物体移动时数值变化正常，旧传感器和升级协议保持不变 |
| `M0.49A` | 实机未通过 | 首版温度传感器协议实现；2 号口的供电及接入电气变化可见，但模块未完成识别。查明其 I²C 节奏快于旧固件，保留此包仅用于问题追溯 |
| `M0.50A` | 实机未通过 | 温度传感器通信已降至旧固件使用的约 50 微秒半周期，但实物仍未完成识别；进一步核对发现尚未复刻旧固件的 I²C 输入通路切换 |
| `M0.51A` | 已实机验证 | 按旧固件的 pin 1/pin 6 电压规则判断 I²C 模块，识别前将 `RXIN_EN` 切到高电平并等待通路稳定；实物确认温度传感器能够自动识别并正常显示温度 |
| `M0.52A` | 已实机验证 | 新增 USB 应用协议 `1.0` 的只读整机快照 `0x19` 和 V5 `PF3` 电池采样；一次返回版本、电量、按键、四个马达及四个输入口状态。修复电脑端超过 64 字节回复的截断后，实机完整读取成功 |
| `M0.53A` | 待实机验证 | LCD 同时显示四路输入和 A-D 马达累计计数；开机自动执行背光全亮、变暗、熄灭、恢复全亮，并通过板载 VS 音频芯片播放两段短测试音 |
| `M0.54A` | 部分实机验证 | LCD 电量显示 `BAT:100%`，背光全亮、变暗、熄灭、恢复全亮均通过；板载 VS 内部测试音可听见且显示 `SND:DONE`，但音量很小，未达到“正常播放”验收标准 |
| `M0.55A` | 部分实机验证 | 改用 WAV 数据通道后实物确认声音明显增大，但实际播放时间很短，没有达到“正常播放一次”的验收标准 |
| `M0.56A` | 实机未通过 | 将 WAV 测试数据延长到约 5 秒后，实物确认播放过程仍不正常。音频问题暂缓，后续需单独核对 VS 数据流结束/填充、时钟和功放控制；本包只用于问题追溯 |
| `M0.57A` | 已实机验证（A/C） | 新增 EST/EV3 MPU6050 陀螺仪类型 `0x20`，支持角度/角速度模式和有符号值显示。A 的启动、USB 和陀螺仪读数正常；C 从原 APP `03.04` 直接升级后正常启动。B 虽完成两次 260 包升级，但启动异常，暂按异常样机处理 |
| `M0.58A` | 已实机验证（C） | 新增 NXT 模拟声音传感器类型 `0x03`；按官方 EST 3.0 规则使用 `LEGDET/pin 2` 低电平识别，并将 pin 1 ADC 换算为 `0..100 dB` 读数。C 设备确认自动识别且声音读数变化正常 |
| `M0.59A` | 已实机验证（C） | 新增 EST/EV3 红外传感器类型 `0x21`；距离、信标及遥控三种 UART 模式均已验收，信标方向/距离和遥控按键代码读取正常 |
| `M0.60A` | 实机未通过 | 首版按官方 `ADCM` 电压窗口区分大型/中型电机，但使用单次 ADC 采样且错误地让保护空档覆盖已确认类型，实机出现 `L/M/?` 显示不稳定；保留用于问题追溯 |
| `M0.61A` | 已实机验证（C） | 修正马达类型稳定性：切换 ADC 后丢弃首个样本并平均 10 次，复刻官方程序在保护空档内保持上一次有效类型的行为；大型/中型马达显示稳定 |
| `M0.62A` | 已实机启动（C），闭环待验收 | 新增类型感知的马达转速估算和单口角度/圈数控制 `0x1B`；C 设备启动及 USB 心跳正常，但 LCD 仍只显示类型和累计计数，无法直接观察圈速、角度和圈数 |
| `M0.63A` | 实机未通过（C） | 首次在 LCD 同时显示转速、角度和圈数，但按错误的 240 像素宽度估算位置；实际 180 像素屏幕上马达栏右侧被裁切，保留用于问题追溯 |
| `M0.64A` | 显示及大/中型角度闭环已验证（C） | 四路显示完整；大型单圈/三圈实测 `+369°/-365°`、`+1084°/-1088°`，中型单圈/三圈实测 `+363°/-366°`、`+1088°/-1089°`，方向、自动完成和停车均正常。热插拔后可能保留旧类型，重启可静止恢复识别；持续定速仍待验收 |
| `M0.65A` | 部分实机验证（C） | C 设备启动、USB 协议 `1.3` 和 D 口中型马达静止刷新通过；B 口大型马达在约 `130 mV` 的编码器相位下仍识别为未知，证明首版 H 桥零差分刷新方法不能消除相位歧义。持续闭环定速尚待验收。SHA-256：`3dedac49d5978a0fb5357591f997321ec8a64c5acf96b708bb3232f54f6bcfae` |
| `M0.66A` | 实机未通过（C） | C 设备正常启动，B 口大型马达静止刷新时确认没有任何转动，但仍返回 `unknown / 129 mV`。pin 6 高/低采样没有解决低编码器相位，保留用于安全性和问题追溯。SHA-256：`23e5187572b0e23e340bec5e92dda2fdebcdfd2287b9dbb713086b59a564faab` |
| `M0.67A` | 实机未通过（C） | B 口大型马达连续三次静止识别为 `large` 且确认没有转动，但 A/C 空口 `129/249 mV` 也被扩展后的 `100..380 mV` 窗口误判为大型。该版只能证明探测过程安全，不能作为类型识别版本。SHA-256：`405bbcdf2a67c0733ab66383572b7154b0e042d7b999d5bd8dd5221dd20dde61` |
| `M0.68A` | 阻抗数据采集完成（C） | 撤销 M0.67A 的空口误判阈值，协议 `1.4` 返回三阶段 ADC。实测弱上拉：A/C 空口 `275/279 mV`、B 大型低相位 `559..562 mV`、D 中型低相位 `428..429 mV`；各组三次结果稳定且全程无驱动。SHA-256：`dcbd542d743ce3a5f486e79fc521d18d0edbf4750498b6f09417677992d45c7b` |
| `M0.69A` | 历史布局曾通过，结论已作废（C） | 当时按 A 空、B 大型、C 空、D 中型标签把弱上拉 `275/560/281/428 mV` 映射为空/大/空/中。M0.71A 的同一马达 A→D 受控换口实验直接证明实际顺序应为大型约 `280 mV`、中型约 `430 mV`、空口约 `560 mV`，因此本版空口/大型窗口反向，不能再作为已验证版本。SHA-256：`872b8b63fe2fcd863fe731a46b7463bcde680518e6a586c45e73f3d3973344b8` |
| `M0.70A` | 实机未通过（C） | 自动轮询、拔出隐藏数据和重插归零逻辑已实现，但 D 空口及随后拔空的 A 都稳定出现约 `249/250/561 mV` 并误显示大型。当时误判为与大型低相位重叠；M0.71A 受控换口后确认根因是弱上拉空口/大型窗口映射反向。SHA-256：`23ec3e90f8b3a09c75bd56e030fbe931f20ac60a25cf96c13e5536e7bb065fb2` |
| `M0.71A` | 实机诊断完成，分类未通过（C） | 微步捕获 A/C 大型高低相位，数字状态只随相位变化。把同一大型马达从 A 移到 D 后，A 空口稳定为 `560..563 mV`，D 大型低相位为 `279..281 mV`，B 中型约 `430 mV`；证明数字位不能单独分类，同时确认 M0.69A 起的空口/大型弱上拉映射反向。本版没有修改分类。SHA-256：`9c828cefd6e2d63cb7eeb4aaf3623ae53d5b4c5fee3b6fa5b53e7eae245c6e32` |
| `M0.72A` | 马达识别、热插拔与持续定速已验证（C） | 按 EST 3.0 官方类别顺序修正弱上拉窗口；空口、中型及大型高低相位均稳定识别，D 拔出实时清除、重插恢复且计数归零。大型/中型 30% 正反向均跟踪目标，大型 60% 正反向平均 `58.8%/-61.9%`；中型负载时输出提升到 100%，卸载后恢复基线。协议 1.5 和安全停止不变；86 项 HID、52 项固件及 144 引脚检查通过。当前没有电流式堵转保护。SHA-256：`576262fbf7d3d13f016a48cbfb5085f5c0c628edca62b5366e4861cbf1c02e00` |
| `M0.73A` | C API 适配实机通过（C） | 新增公共类型、单电机/底盘契约和 `est_motor` 适配层；原 HID 功率、角度、定速、停止与清零命令改经服务层。实机 10% 点动正常，D 中型 `+60°` 目标/实际均为 60°，C 大型 30% 定速平均 `27.9%`，最终全部安全停止且类型稳定。`run_time`、位置保持和双电机同步仍未实现。56 项固件、86 项 HID、ARM `-Werror`、144 引脚和包校验通过。SHA-256：`7fe2431c77b038d0c7408fc9a22e5078f36ddf782d25880156fabe3e96ac86aa` |
| `M0.74A` | 多端口独立闭环实机通过（C） | 将速度与位置闭环从单实例改为 A-D 四端口独立状态。C 大型与 D 中型并发 `+30%/-30%` 时实测 `+33%/-31%`；单独停止 C 后 D 仍以 `-29%` 继续运行。并发 `+180°/-180°` 分别到达 `+184°/-196°`，状态独立完成。测试后四路均为自由滑行、功率 0，类型稳定。57 项固件、86 项 HID、ARM `-Werror`、144 引脚和包校验通过。SHA-256：`1cd7e9a671142885713efd64d10cc748b52578758da11bf02fa8982c51d10f6c` |
| `M0.75A` | 双大型马达同步实机通过（C） | 新增 `est_drive` 首版相同绝对角度同步、HID `0x1D` 和联动超时停止。A/C 双大型反向一圈最大同步误差 `5°`；同向 20% 五圈均到 `1814°`，最大误差 `7°`。短暂受阻可恢复，持续压住 C 会触发超时并使两路自由滑行。API 允许混合类型，但正式同步验收使用同型号马达。58 项固件、89 项 HID、ARM GCC 12.2.1 `-Werror` 和包校验通过。SHA-256：`9765cddf54a88de34d08a35ecdfacbdab864757bfd1377cd4e4d0a16a4d0f880` |
| `M0.76A` | 双电机无自动超时实机通过（C） | 按产品要求取消双电机任务的自动时间上限和电脑端 18 秒截止时间，单电机超时不变。A/C 双大型同向 20% 十圈运行约 20 秒仍到 `3606°/3606°`；对 A 施加外力后松开，可继续追赶并以 `2707°/2708°` 完成且无 fault。显式停止、升级和关机仍同时关闭两路。58 项固件、90 项 HID、ARM GCC 12.2.1 `-Werror` 和包校验通过。SHA-256：`3b940f5f7e1323b9ac6b4802e4895f9af6dc9f691b5c2ddf6057304ba97aec92` |
| `M0.77A` | 双电机速度跟随有效，释放后反超待优化（C） | A 受力时 C 会随 A 明显降速，证明编码器实时速度跟随有效；两轮 A/C 同向 `2700°`、20% 测试均完成且无 fault，最大同步误差分别为 `174°` 和 `112°`，低于 M0.76A 的 `322°`。但 A 受阻期间闭环累积高 PWM，释放后会追赶过快并反超，随后两侧交替追赶，不能作为最终同步参数。58 项固件、90 项 HID、ARM GCC 12.2.1 `-Werror` 和包校验通过。SHA-256：`3660a5ded813c50f7aae67ff1db46f73ecd213729b253b599c7b9a05a925fc22` |
| `M0.78A` | 无反超但动力与同步不通过（C） | A 受力松开后没有再明显反超，但落后侧恢复限速和硬 PWM 限制导致动力显著不足，两台马达低速走走停停。A/C 同向 `2700°`、20% 虽以 `2702°/2703°` 完成且无 fault，最大同步误差 `136°`，但总耗时约 61 秒，远慢于 M0.77A 的约 17 秒。该参数不能验收。58 项固件、90 项 HID、ARM GCC 12.2.1 `-Werror` 和包校验通过。SHA-256：`dbc56aa35ae602536f72b81591de1442b5e4d137701cf29aea15680a68b10ee1` |
| `M0.79A` | 动力部分恢复但无负载仍偏慢（C） | 撤销 M0.78A 的硬限功率后，A/C 无外力同向 `2700°`、20% 以 `2704°/2704°` 完成，最终误差 `-2°`、最大同步误差 `27°`，无 fault；总耗时约 27 秒，明显好于 M0.78A 的 61 秒，但仍慢于正常约 17～20 秒。根因是实测速度只超过目标 3 个百分点就触发 4 倍退积分，普通编码器波动也会频繁收油，不能验收。58 项固件、90 项 HID、ARM GCC 12.2.1 `-Werror` 和包校验通过。SHA-256：`e3cb7e6f7ef65aa83e8030a3c8a4bb1b80859e9a6294f68d91dacb601b6833b4` |
| `M0.80A` | 无负载通过，卸载反冲未通过（C） | A/C 无负载同向 `2700°`、20% 用时约 15.2 秒，以 `2710°/2710°` 完成，最大同步误差仅 `7°`，常态 PWM 约 21%～24%，证明正常动力已恢复。外力测试中 C 会跟随受力的 A 降速，但 A 的 PWM 累积到 79%～91%，释放后实际速度瞬间达到 41%～45%，最大同步误差 `157°`；随后虽能恢复并以 `2698°/2703°` 完成，仍不能验收。SHA-256：`554b4be0337a767b71804f7fe9524bf8bc006284b522898861165520f4e315b8` |
| `M0.81A` | 卸载判断误触发，实机失败（C） | 无负载同向 `2700°`、20% 仍能在约 14.8 秒以 `2706°/2706°` 完成，最大误差 `6°`；但外力测试中，跟随侧尚未降到临时目标速度就被误判为卸载并清零输出，造成两侧走停和角色反复切换。真正释放时 A 已累积至 100% PWM，慢速平均测速直到 A 达到 99% 才发现，最大同步误差扩大到 `241°`，最终 `2727°/2702°`。本版逻辑已废弃。SHA-256：`189cba7dcbdf1690c618e5f1ad6a52c860c6a64d2ee021bc3e6bbed706d54ea1` |
| `M0.82A` | 回退至 M0.80A 控制逻辑，待实机确认（C） | 完整删除 M0.81A 的卸载状态、PWM 立即清零和 2%～5% 追赶补偿，恢复 M0.80A 的正常动力、受力速度跟随、4° 角色回差和超过有效目标 10 个百分点时的快速退积分；实时速度/PWM 遥测和双电机无自动超时保持不变。58 项固件、90 项 HID、ARM GCC 12.2.1 `-Werror` 和严格包校验通过。SHA-256：`8d92081b290e249b586037d45192e02d51a90a81a4bb69c58fd028e0f425db24` |
| `M0.83A` | 双电机持续定速实机通过（C） | 在 M0.80A 同步逻辑基础上新增一组双电机持续闭环定速、EST API、HID `0x1E` 和协议 `1.7`。固件不设运行时限，只有显式自由滑行、刹车、升级或关机才停止两路；允许用户明确选择大型/中型混合配对，首版要求两路目标速度绝对值相同但方向可独立。A/C 双大型同向 20% 和 40% 的最大同步误差均为 `5°`；反向 `+40%/-40%` 启动最大误差 `10°`，稳定后为 `0°..3°`；同向 80% 实测稳定在 `79%..81%`，最大误差 `5°`，两路主动刹车同步生效。40% 外力测试中跟随减速、释放恢复及显式停止正常。59 项固件、93 项 HID、ARM GCC 12.2.1 `-Werror` 和严格包校验通过。SHA-256：`cace43967a9f9de2ae9db1d1a22f6ce53160d64c8d00b1f96d4d9a9bde63a6d9` |
| `M0.84A` | 毫米直行链路通过，产品方向已调整（C） | 实现 `est_drive_straight()`，在 STM32 C 层使用 `355/113` 圆周近似把有符号毫米距离按轮径换算为两轮相同目标角度。A/C 以 56 mm 轮径执行 500 mm、40% 测试，固件目标 `1023°`，实际 `1029°/1028°`，最大同步误差 `6°`，回报约 `502/500 mm` 并安全自由滑行。用户随后明确最终界面只需要圈数、角度和秒数，因此本版保留为链路诊断，不作为正式直行交互。60 项固件、96 项 HID、ARM GCC 12.2.1 `-Werror` 和严格包校验通过。SHA-256：`77144ad526b03395cd502d51d14de2c2759f465405c17c170c7c506086004be8` |
| `M0.85A` | 圈/度/秒直行实机通过（C） | 新增统一 `drive-run`、HID `0x20` 和协议 `1.9`。圈数由上位机按 `1 圈 = 360°` 换算，角度由双电机位置同步执行；秒数由固件内部计时，到时自动自由滑行或主动刹车，不依赖 USB 按时发送停止命令。A/C 双大型以 40% 执行 `+720°`，最终 `728°/720°`、最大同步误差 `21°`；反向 `-2.5` 圈转换为 `-900°`，最终 `-907°/-906°`、最大误差 `6°`；60% 定时 3 秒在 `3000/3000 ms` 自动完成，两侧均累计 `1680°`、最大误差 `13°`。61 项固件、99 项 HID、ARM GCC 12.2.1 `-Werror` 和严格包校验通过。SHA-256：`d36d33fe83278d24e6ccf9cd62a2103164b72453828ff1c8c508571a7dcd939f` |
| `M0.86A` | 双电机不同行程比例同步实机通过（C） | 保持 HID `0x1D` 格式不变，将双电机角度任务扩展到左右各自非零 `±3600°`。长行程侧使用指定最大速度，短行程侧按目标比例降低基础速度；进度和同伴限速均投影到较长目标的等效尺度。同向 `720°/360°` 最终 `722°/368°`、最大等效误差 `26°`；反向 `720°/-360°` 两轮最终分别为 `723°/-366°` 和 `727°/-365°`、最大误差 `18°/20°`。用户确认行程比例正确且基本同时停下。协议 `1.10`，61 项固件、99 项 HID、ARM GCC 12.2.1 `-Werror` 和严格包校验通过。SHA-256：`388eedc7b8c197653841c7ef9097746bf5a1378e7958575aa245a36b4b6e1e87` |
| `M0.87A` | 左右独立持续速度实机通过（C） | 保持 HID `0x1E` 格式不变，将双电机持续闭环定速扩展为左右独立的非零 `10..100%` 有符号速度。编码器进度、同伴限速和纠偏量均按目标速度比例投影。A/C 双大型 `40%/20%` 最终 `3089°/1542°`、比例约 `2.003:1`、最大等效误差 `18°`；`80%/40%` 最终 `6200°/3102°`、比例约 `1.999:1`、最大误差 `25°`。用户确认肉眼表现无问题，停止后四路功率为 0。协议 `1.11`，61 项固件、99 项 HID、ARM GCC 12.2.1 `-Werror` 和严格包校验通过。SHA-256：`2fabc36ede9e8352d6c55f185af6121c6f6b23cc8378c046e8ba9a3802731275` |
| `M0.88A` | EV3 持续转向基础实机通过（C） | 新增 EV3 Classroom 语义的持续转向 C API、HID `0x21` 和电脑端 `drive-steer`。C 设备完成 260/260 帧升级后正常启动，心跳为 M0.88A、协议 `1.12`；A/C 双大型以移动速度 80、转向 `+50` 运行时有效目标为 `80%/40%`，最终 `2230°/1120°`、比例约 `1.99:1`、最大等效误差 `16°`，用户确认表现正常并安全自由滑行。转向 `-50`、反向移动和 `±100` 原地旋转尚未分别实测。62 项固件、102 项 HID、ARM GCC 12.2.1 `-Werror` 和严格包校验通过。当前闭环不支持换算后任一侧绝对速度低于 10%。SHA-256：`d3a4645a950aa5c4c6276fb4d8c751089a3ecf8995370794880c110fed637d25` |
| `M0.89A` | LCD 冷启动兼容修复通过，高负载待锂电复测 | 放宽 UC1638 的复位和控制器稳定等待时间；当前主机在 M0.88A 下只有背光、彻底断电后仍无文字，升级 M0.89A 后正常显示且 USB 心跳/协议 `1.12` 正常。A/B 双大型完成转向 `-50`、反向移动及 `±100` 原地旋转的软件链路测试。使用干电池时闭环倒车抖动，双马达固定 `-80%` 两次均使整机关机，但 A、B 分别固定 `-80%` 可稳定运行约 `-1558/-1572` 计数，初步指向干电池高负载压降；该项等待锂电池复测，不能据此修改马达算法。63 项固件测试、144 引脚检查、ARM GCC 12.2.1 `-Werror` 和严格包校验通过。SHA-256：`e7b35a9304e69eb91db7a108dfe0abcad292e53b6017feeec5e57b1967c241f2` |
| `M0.90A` | 定量转向已构建，待锂电实机验收 | 新增 `est_drive_steer_for()`、HID `0x22`、协议 `1.13` 和电脑端 `drive-steer-for`。保持 `0x21` 持续转向不变，按同一 EV3 公式换算左右速度；角度/圈数模式把外侧轮目标按速度比例换算为左右独立目标角度，时间模式由固件内部计时并自动停止。顺手修复 Windows 全新构建目录不支持 `mkdir -p` 的问题。64 项固件测试、105 项 HID 测试、ARM GCC 12.2.1 `-Werror` 和固定 256 KiB 包校验通过；暂未在干电池设备上运行马达，等待锂电池后验收。SHA-256：`f4beb1030288307980909a1ff21c5d09be104215b659137ac71e3e8a52a2c6b6` |
| `M0.91A` | 传感器服务层已构建，待实机回归 | 新增 `est_sensor.h/.c`，统一类型、状态、模式、值格式、模式切换、端口重启和状态查询；现有 HID `0x18` 与 `0x19` 传感器路径改经该服务层。温度和陀螺仪在 C API 中按有符号数解释，同时保留原始类型和值供旧 USB 帧原样返回；协议仍为 `1.13`。65 项固件测试、105 项 HID 测试、144 引脚检查、ARM GCC 12.2.1 `-Werror` 和固定 256 KiB 包校验通过。SHA-256：`c4932289de6a38a0fa3aadf9e990974f2321eca4ed5b2d895b85f296bbdefbf6` |

目录内只保存可交给升级工具的 `.upgrade.bin` 和对应 manifest。不要发送 `M0.09A`、存在 LCD/USB 引脚冲突的 `M0.24A`，也不要发送构建目录中的原始 `.bin`。
