# 已验证固件发布包

| 版本 | 状态 | 用途 |
| --- | --- | --- |
| `M0.16A` | 已实机验证 | 使用官方 MSP 的长入口信标版本，适合启动链路诊断 |
| `M0.17A` | 已实机验证 | 约 2 秒入口签名，之后仅蓝灯慢闪；当前设备运行版本 |

目录内只保存可交给升级工具的 `.upgrade.bin` 和对应 manifest。不要发送 `M0.09A`，也不要发送构建目录中的原始 `.bin`。

