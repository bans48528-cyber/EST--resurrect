# EST 项目说明与当前工作范围

日期：2026-08-26

本文用于说明当前 EST 主控重构项目的真实状态、工作目标和边界。若本文与早期迁移记录或旧分支对话冲突，应优先采用本文和当前工作区文件；尤其应采用 `docs/EST_Bootloader_对话迁移上下文_2026-08-21.md` 最前面的“最新迁移覆盖段（2026-08-21，必须优先采用）”。

## 最新覆盖（2026-08-26，M0.83A 双电机持续定速验收通过，必须优先采用）

- 当前 C 设备已安装并正常运行 `M0.83A`，USB 心跳正常，协议为 `1.7`，四路马达输出均为自由滑行。当前诊断布局为 A 大型、B 空、C 大型、D 中型。A、C 是可靠开发样机；B 启动异常，已退出通用固件验证范围。
- 声音输入 `M0.58A`、红外 `M0.59A` 已在 C 设备实机通过。红外距离、信标和遥控三种模式均已验收，不再属于“没有样品/未测试”。
- 当前工作已从基础硬件枚举进入马达闭环控制：分别为大型和中型马达建立转速估算、角度和圈数控制参数，不修改现有 `03.00B` Bootloader。
- `M0.62A` 已新增 HID 命令 `0x1B` 和电脑端 `motor-position`。角度按编码器 `1 计数 = 1°`，圈数统一按 `360°` 换算；大型/中型马达分别采用官方测速基准 `12800/8100` 和采样窗口 `4/16/32/64`、`2/4/8/16`。
- 位置控制沿用官方短行程限速、减速角和每 10 ms 的增量比例控制参数，限制单次 `±3600°`，到位短暂主动刹车后回到自由滑行，异常时超时停机；未知电机类型会拒绝启动。
- `M0.62A` 的 LCD 仍只显示马达类型和累计计数。`M0.63A` 首次加入转速、角度和圈数，但实机发现仍按错误的 240 像素估算排版：实际 LCD 宽度为 180 像素，马达栏从 `x=96` 开始只能容纳 14 个字符，因此右侧内容被裁切。
- `M0.64A` 把马达栏整体左移到 `x=72`，标题改为 `MTR S% DEG REV`，数据采用最多 18 个字符的紧凑格式，例如 `AL-30 -360 -1.0`。代码新增编译期边界检查，保证整行落在 180 像素范围内。
- `M0.64A` 已通过 ARM GCC `-Werror` 构建、升级包布局校验和 49 项固件测试；C 设备已完成 260/260 帧升级，重启后心跳返回 `M0.64A`，用户确认四路马达内容完整显示且没有问题。LCD 修改未改变马达控制或 HID 协议。发布包 SHA-256 为 `d6136010cd14234b931724706a644011f31adabeb782bcf42d1aad958cdbabed`。
- C 设备 B 口大型马达已完成 `30% / +360°` 和 `30% / -360°` 闭环测试：实际位移分别为 `+369°/-365°`，超调 `9°/5°`，两次均自动完成并回到自由滑行；用户确认正反方向和停车表现均无问题。
- C 设备 B 口中型马达已完成相同测试：实际位移为 `+363°/-366°`，超调 `3°/6°`，两次均自动完成并回到自由滑行；用户确认正反方向和停车表现均无问题。
- 中型马达随后以 30% 完成 `+3/-3` 圈测试：实际位移为 `+1088°/-1089°`，超调 `8°/9°`，方向、圈数和停车均由用户确认正常。
- 大型马达以 30% 完成 `+3/-3` 圈测试：实际位移为 `+1084°/-1088°`，超调 `4°/8°`，方向、圈数和停车均由用户确认正常。
- 大型与中型马达的单圈和三圈角度闭环均已通过。运行中把 B 口由中型换为大型后，当前 ADC 曾落在约 `130 mV` 的保护空档并保留旧类型；保持大型马达连接并重启后，不转动马达即可重新识别为大型，电压 `2867 mV`。
- `M0.65A` 已刷入 C 设备并正常启动；该版新增大型/中型马达持续闭环定速命令 `0x1C` 和电脑端 `motor-speed`，支持 `-100..-10`、`10..100` 有符号目标转速及自由滑行/主动刹车停止。定速、位置、诊断和类型刷新互斥，升级及关机路径仍强制关闭全部输出；该功能后续已在 M0.72A 实机验收。
- `M0.65A` 的首版静止刷新方法已实机判定不完整：D 口中型马达可识别为 `medium`，电压约 `1849 mV`；B 口大型马达在当前编码器机械相位下仍为 `unknown`，电压约 `130 mV`。原因是该版对 H 桥施加零差分电平，却没有改变真正参与类型检测的编码器 pin 6，因此不能消除 pin 5 高低相位造成的歧义。
- `M0.66A` 已刷入 C 设备并正常启动，心跳返回 `M0.66A`。B 口静止刷新确认马达完全不动，但结果仍为 `unknown / 129 mV`；这证明 H 桥关闭的安全目标达成，而 pin 6 高/低采样和原来的大型低相位窗口仍不能覆盖当前实物。
- LEGO EV3 原始马达检测源代码明确采用“pin 6 浮空时采样 pin 5，再把 pin 6 拉低并再次采样”的顺序，并把高于近地阈值 `100 mV`、低于中型阈值的区域归入大型测速马达。B 口实测 `129 mV` 正落在该范围。
- `M0.67A` 已改为官方的 pin 6 浮空/拉低两阶段采样，并把大型马达低相位窗口扩展为 `100..380 mV`；中型 `400..480 mV`、大型/中型高相位窗口及空口窗口保持不变。H 桥全程关闭，不发出马达转动命令。
- `M0.67A` 已通过 ARM GCC `-Werror` 构建、升级包布局/manifest 校验、86 项 HID 工具测试、50 项固件测试和 144 引脚冲突检查。固定 256 KiB 升级包 SHA-256 为 `405bbcdf2a67c0733ab66383572b7154b0e042d7b999d5bd8dd5221dd20dde61`。
- C 设备已完成 `M0.66A -> M0.67A` 的 260/260 帧升级并正常启动。B 口大型马达在不转动的情况下连续三次识别为 `large`，识别电压约为 `2864..2869 mV`；但用户随后发现未接马达的 C 口也显示为大型。USB 只读结果进一步确认 A 空口 `129 mV`、C 空口 `249 mV` 均被误判为大型，B 大型为 `2867 mV`，D 中型为 `1844 mV`。因此 M0.67A 只证明了刷新过程不转动马达，类型识别没有通过。
- M0.68A 已撤销 M0.67A 的 `100..380 mV` 大型低相位窗口，避免仅凭低电压把空口判为大型；新增 pin 6 浮空、pin 6 拉低、pin 5 弱上拉三组 ADC 数据回报，协议版本升为 `1.4`。弱上拉只作用于编码器信号输入，H 桥保持关闭，并在探测后恢复输入模式及原测速计数。
- C 设备已升级到 `M0.68A` 并完成三阶段阻抗采集，全程没有马达驱动。A/C 空口的“浮空/拉低/弱上拉”分别约为 `130/131/275 mV`、`131/133/279 mV`；B 大型低相位连续三次约为 `249..250/251..252/559..562 mV`；D 中型低相位连续三次约为 `185..186/189..190/428..429 mV`。三类弱上拉结果没有重叠。
- `M0.69A` 保留原高相位电压窗口，仅在浮空和拉低电压都小于 `300 mV` 时启用弱上拉判据：空口 `200..350 mV`、中型 `380..500 mV`、大型 `520..650 mV`，各档之间保留保护空白区。三组诊断数据继续通过协议 `1.4` 返回。
- `M0.69A` 已通过 ARM GCC `-Werror` 构建、升级包布局/manifest 校验、86 项 HID 工具测试、50 项固件测试和 144 引脚冲突检查。固定 256 KiB 升级包 SHA-256 为 `872b8b63fe2fcd863fe731a46b7463bcde680518e6a586c45e73f3d3973344b8`。
- C 设备已完成 `M0.68A -> M0.69A` 的 260/260 帧升级，重启后心跳返回 `M0.69A`。保持 A 空、B 大型、C 空、D 中型布局连续执行三轮静止刷新，三轮均得到 `none/large/none/medium`，且全部报告 `identification_motion=none`。弱上拉实测范围为 A 空口 `275..278 mV`、B 大型 `559..561 mV`、C 空口 `281 mV`、D 中型 `427..429 mV`；静止类型识别正式验收通过。
- `M0.70A` 已实现静止热插拔自动识别：四路输出均为自由滑行且马达静止时，以 100 ms 间隔轮询 A-D 的三阶段阻抗，单口最迟约 0.4 秒开始下一轮；拔出后类型变为空口并清除速度、角度和圈数显示，重新插入或换型时测速计数从 0 开始。任何控制命令会先取消后台探测，运行中的编码器引脚不会被切换。
- `M0.70A` 已通过 ARM GCC `-Werror` 构建、升级包布局/manifest 校验、86 项 HID 工具测试、51 项固件测试和 144 引脚冲突检查。固定 256 KiB 升级包 SHA-256 为 `23ec3e90f8b3a09c75bd56e030fbe931f20ac60a25cf96c13e5536e7bb065fb2`；C 设备已完成 260/260 帧升级并返回 M0.70A 心跳。
- M0.70A 实机热插拔没有通过：D 空口连续三次稳定返回常态/拉低约 `246..249 mV`、弱上拉 `559..561 mV`，被错误判为大型；把 A 口大型马达移到 D 后，刚拔空的 A 同样返回 `249..250/249..250/561..563 mV` 并误判大型。当时把该结果误解为“空口与大型低相位重叠”，M0.71A 后续受控换口已证明根因是弱上拉空口/大型窗口映射反向。
- `M0.71A` 在 pin 5 弱上拉阶段额外读取该 GPIO 的数字高低状态，并将协议升为 `1.5`；马达分类暂不使用该新字段，先采集 A 空口与 D 大型马达实测。57 字节类型回包恰好装入一个 64 字节逻辑帧，电脑端继续兼容旧 21/53 字节回包。
- `M0.71A` 已通过 ARM GCC `-Werror` 构建、升级包布局/manifest 校验、86 项 HID 工具测试、51 项固件测试和 144 引脚冲突检查。固定 256 KiB 升级包 SHA-256 为 `9c828cefd6e2d63cb7eeb4aaf3623ae53d5b4c5fee3b6fa5b53e7eae245c6e32`；C 设备已完成 260/260 帧升级，重启后心跳返回 M0.71A。
- M0.71A 用 `10% × 0.1 秒` 微步取得大型马达完整相位：A 大型在高相位 `2862..2866/2883..2884 mV`、数字高与低相位 `133..135/281..285 mV`、数字低之间切换；C 大型同样在约 `2890/2909 mV` 与 `130/278 mV` 之间切换。数字状态只能反映高低相位，不能单独区分空口。
- 随后把同一台大型马达从 A 移到 D，三轮静止刷新均得到 A 空口 `250..251/560..563 mV`、D 大型低相位 `129..133/279..281 mV`；B 中型约 `194/430 mV`。数据随插接状态交换而不是随端口固定，直接证明 M0.69A 起采用的弱上拉“空口低、大型高”映射反了。此前按旧布局标签得出的 M0.69A 验收结论作废。
- EST 3.0 官方依据位于 `EST 3.0/EST 主控源代码/Project/src/Output/outputworkmode.c` 的 `ChannelMotorCheck()`；五份主控工程均规定低相位从小到大依次为大型 `300..380 mV`、中型 `400..480 mV`、空口 `500..1000 mV`，并保留大型/中型高相位窗口。
- `M0.72A` 保留 M0.71A 的三阶段安全扫描和协议 `1.5`，仅把弱上拉窗口修正为大型 `250..360 mV`、中型 `380..500 mV`、空口 `520..1000 mV`，各类型之间保留保护空档。Makefile 同时修复版本参数变化时 `app_version.o` 不重编译的问题。
- `M0.72A` 已通过 ARM GCC `-Werror` 构建、升级包布局/manifest 校验、86 项 HID 工具测试、52 项固件测试和 144 引脚冲突检查。固定 256 KiB 包共 260 帧，SHA-256 为 `576262fbf7d3d13f016a48cbfb5085f5c0c628edca62b5366e4861cbf1c02e00`。C 设备已完成 M0.71A 到 M0.72A 的 260/260 帧升级，重启后心跳返回 M0.72A。
- M0.72A 在 A 空、B 中型、C/D 大型布局下连续四轮稳定识别为 `none/medium/large/large`：空口弱上拉约 `557..561 mV`，中型约 `427..430 mV`，大型低相位约 `275..281 mV`。C、D 分别以 `10% x 0.1 秒` 微步后，C 在低相位约 `275 mV`、D 在高相位约 `2875..2878 mV`，两者仍稳定识别为大型。
- M0.72A 的实时拔插和归零已通过：开机拔出 D 口大型马达后连续三轮立即显示为空口，弱上拉约 `560..561 mV`；重新插入后连续三轮恢复为大型，D 口状态为自由滑行、功率 0、测速计数 0。马达类型显示、拔出清除和重插归零功能正式验收通过。
- M0.72A 的持续闭环定速已在当前 A 大型、B 空、C 大型、D 中型布局下验收。30% 正反向测试中，D 中型末速为 `31%/-31%`，C 大型末速为 `30%/-30%`；四次均正常结束并回到自由滑行。60% 持续 8 秒测试中，D 中型无额外负载的反向平均 `-59.8%`，C 大型正反向平均 `58.8%/-61.9%`。
- D 中型已完成 60% 受控负载测试：无外力平均 `57.5%`、输出约 `65%`；施加阻力后控制器把输出提高到 `100%`，平均转速为 `52.0%`；卸载后平均恢复到 `59.1%`，输出回落到 `65%`。这证明负载补偿和卸载恢复有效，同时也确认当前没有电流式堵转保护，达到 100% 后无法继续补偿，不应长时间捏停马达。
- 基础硬件、马达识别、热插拔、位置闭环和单电机持续定速已形成可用基线。下一阶段按 `docs/EST_C_API与MicroPython下一步工作清单.md` 建立正式 C API：先冻结公共类型和单电机接口，再把现有 `board_motor_*` 接入适配层。
- `M0.73A` 已完成 C API 第一阶段：新增 `est_types.h`、`est_motor.h`、`est_drive.h` 和 `est_motor.c`；HID 的开环功率、角度、持续定速、停止与清零命令已改经 `est_motor_*` 适配层，原协议格式和 `board_motor` 算法不变。按时间运行、位置保持和双电机同步尚未实现，不会静默降级。
- M0.73A 已通过 ARM GCC `-Werror` 构建、56 项固件规则测试、86 项 HID 工具测试、144 引脚检查和升级包严格校验。固定 256 KiB 包共 260 帧，SHA-256 为 `7fe2431c77b038d0c7408fc9a22e5078f36ddf782d25880156fabe3e96ac86aa`。C 设备已完成 260/260 帧升级并返回 M0.73A 心跳。
- M0.73A 的 C API 适配实机回归已通过：C 大型以 10% 点动 0.1 秒产生 6 个计数并滑行停止；D 中型以 20% 执行相对 `+60°`，目标和实际均为 `60°`、误差 0；C 大型以 30% 持续定速 2 秒，平均 `27.9%`、末速 `28%`。测试后 A-D 均为自由滑行、功率 0，马达类型仍稳定为 `large/none/large/medium`。
- `M0.74A` 已把速度和位置闭环从全局单实例改为 A-D 每端口独立状态。一路定速/位置控制不再阻塞其他端口，逐端口停止不会关闭正在运行的另一路；同端口命令互斥、旧诊断全局互斥、升级/急停关闭全部输出的安全边界保持不变。
- C 设备实机并发定速已通过：C 大型与 D 中型同时目标 `+30%/-30%`，实测 `+33%/-31%`；单独停止 C 后，C 功率归零，D 仍以 `-29%` 继续运行并新增反向计数，随后 D 也安全停止。
- C/D 并发角度目标 `+180°/-180°` 分别以 `+184°/-196°` 完成，D 先到位不会改写 C 的任务状态。测试后 A-D 全部为自由滑行、功率 0，马达类型仍稳定为 `large/none/large/medium`。
- M0.74A 已通过 ARM GCC `-Werror` 构建、57 项固件规则测试、86 项 HID 工具测试、144 引脚检查、升级包严格校验和 C 设备实机验收。固定 256 KiB 包的 SHA-256 为 `1cd7e9a671142885713efd64d10cc748b52578758da11bf02fa8982c51d10f6c`。
- `M0.75A` 新增 `est_drive.c`、双路编码器进度协调器、HID 命令 `0x1D` 和电脑端 `motor-pair-position`。首版要求两路目标角度绝对值相同，允许同向或反向；只降低领先马达的目标速度，不提高落后马达速度。启动、停止和超时均联动处理，状态回报包含当前及最大同步误差。
- 正式同步基准使用同型号马达，固件和 API 不强制类型相同，用户仍可显式组合大型和中型。A/C 两个大型马达反向 `+360°/-360°` 实测 `+367°/-366°`，全程最大同步误差 `5°`；同向 20% 五圈目标 `1800°/1800°`，两路均到 `1814°`，全程最大误差 `7°`。
- 抗扰测试中，短暂按压 A 后两路可恢复并完成；持续按压 C 会使误差扩大并触发超时，固件随后联动关闭两路输出并回到自由滑行。大型/中型混合组合可运行，但不作为同步精度基准。
- M0.75A 已通过 ARM GCC 12.2.1 `-Werror` 构建、58 项固件规则测试、89 项 HID 工具测试和升级包严格校验。固定 256 KiB 包的 SHA-256 为 `9765cddf54a88de34d08a35ecdfacbdab864757bfd1377cd4e4d0a16a4d0f880`。
- `M0.76A` 按产品要求取消双电机同步任务的自动时间上限和联动超时停机。受阻电机松开后继续追赶目标；任务只在两路到位、用户显式停止、升级或关机时结束。单电机位置命令原有超时保持不变，电脑端 `motor-pair-position` 同时取消原 18 秒等待上限，并在退出路径保留显式自由滑行。
- M0.76A 无外力同向 20% 十圈运行约 20 秒，超过旧版 15 秒上限后仍以 `3606°/3606°` 完成，全程最大同步误差 `9°`。随后同向 20% 运行 `2700°/2700°`，用户对 A 施加外力使其停在约 `1995°`，C 继续前进；松开 A 后它重新追赶，最终以 `2707°/2708°` 完成且没有进入 fault。
- M0.76A 已通过 ARM GCC 12.2.1 `-Werror` 构建、58 项固件规则测试、90 项 HID 工具测试和升级包严格校验。固定 256 KiB 包的 SHA-256 为 `3b940f5f7e1323b9ac6b4802e4895f9af6dc9f691b5c2ddf6057304ba97aec92`。
- M0.77A-M0.81A 用于研究受力时跟随和释放后反冲。M0.77A 能让未受力侧跟随降速但释放后追赶过快；M0.78A-M0.79A 降低反冲的同时损失过多动力；M0.81A 的卸载判断发生误触发并造成走停。上述参数均未验收，M0.82A 已完整回退至 M0.80A 控制逻辑但按用户决定不单独刷写验证。
- `M0.83A` 新增 `est_motor_pair_run_speeds()`、`est_motor_pair_stop()` 和持续状态查询，HID 命令为 `0x1E`。系统只允许一组配对任务；首版要求两路目标速度绝对值相同，方向可独立。默认产品选择应优先同型号马达，用户显式选择大型/中型混合组合时固件不拒绝。
- M0.83A 的 A/C 双大型持续定速实机通过：同向 20% 和 40% 最大同步误差均为 `5°`；反向 `+40%/-40%` 启动最大误差 `10°`，稳定后通常为 `0°..3°`；同向 80% 实测保持 `79%..81%`，PWM 约 `89%/91%`，最大误差 `5°`。40% 外力测试中另一侧会跟随减速，释放后恢复；用户确认 80% 时两路主动刹车同时快速生效。
- 双电机持续定速在固件中没有自动超时，只能通过显式自由滑行、主动刹车、升级或关机结束。电脑端 `--duration` 仅定义观察时间，结束后必须发送明确停止命令。M0.83A 已通过 59 项固件测试、93 项 HID 测试、ARM `-Werror` 和严格包校验，SHA-256 为 `cace43967a9f9de2ae9db1d1a22f6ce53160d64c8d00b1f96d4d9a9bde63a6d9`。

当前工作内容明确为：维护可持续升级的新 GCC APP、建立稳定的硬件驱动和控制协议，并逐项用 A/C 实物验收。旧官方 APP 只作为协议和参数证据，B 异常样机不用于判断通用固件成败。下文早期版本状态保留作历史追溯，若有冲突，以本节为准。

## 上一覆盖（2026-08-25，M0.57A）

- 已通过旧资料和设备回报确定 1 号输入口接入的是 MPU6050 陀螺仪模块，传感器类型为 0x20。M0.56A 能收到类型声明，但旧实现不接受该类型，因此停留在 sensor_state=syncing。
- 已完成 M0.57A 源码支持：接受类型 0x20，支持角度/角速度模式 0/1，按有符号 16 位值显示，并在 HID 工具中增加 angle、rate 和负值解释；模式 2 会被拒绝，避免发送陀螺仪不支持的模式。
- M0.57A 已通过 45 项固件测试、74 项 HID 工具测试、ARM GCC 编译、APP= 包头、向量表、固定 256 KiB 升级包和 manifest/SHA-256 校验。
- A 设备已从 M0.56A 直接升级到 M0.57A，启动、USB HID、陀螺仪角度和角速度读取均正常。C 设备原 APP 心跳为 03.04，在与 B 相同的底层引导上直接升级到 M0.57A 后也能正常启动。
- B 设备在 03.00B Bootloader 下两次完整接收 M0.57A（260/260、done），但正常开机仍停在启动动画并保持蓝灯常亮，且没有 USB HID 枚举。由于相同固件已在 A、C 正常运行，B 暂定为异常样机，不再作为通用固件开发基线。

## 0. 当前实物状态（2026-08-26）

- 当前 C 设备实机版本为 `M0.83A`；当前诊断布局为 A 大型、B 空、C 大型、D 中型。A 是可靠备用样机，B 暂定为异常样机。当前版本用于逐项验证硬件驱动和马达控制，不代表最终产品固件。
- 全新 GCC APP 已完成旧引导兼容、重复升级、USB HID 通信和逐包 ACK 的闭环验证；旧固件主体不再作为后续产品固件基础。
- 已通过实物测试的主要功能包括：LCD、六个按键、LED、电池、背光、W25Q256 外部 Flash、A-D 马达驱动与转动计数、1-4 输入口，以及颜色/灰度、触碰、超声波和温度传感器。
- 音频链路能够出声，增加音量和播放数据长度后仍存在播放过程异常，因此音频没有通过验收，按用户决定暂缓处理。
- MPU6050 陀螺仪已在 A 设备 1 号口完成类型识别、角度和角速度实测；声音输入和红外传感器已在 C 设备完成实测，其中红外的距离、信标和遥控三种模式均已验收。
- 分项结论及后续边界统一见 `docs/EST基础硬件验收清单_2026-08-25.md`，升级包状态统一见 `firmware/releases/README.md`。

## 0.1 上一阶段记录（2026-08-24）

- 当前设备运行 `M0.43A`，仍使用原 `03.00B` Bootloader；正常 APP 模式可直接接收下一版升级包。
- A-D 四路输出的 PWM、方向控制和测速接线均已完成实机验证。通用命令 `0x17` 支持端口选择、`-100%..+100%` 有符号功率、自由滑行、主动刹车、测速读取和计数清零，不改变旧升级协议、帧格式或 ACK。
- A 与 D 已验证可同时保持不同方向和功率：A `+30%` 计数 `+246`，D `-30%` 计数 `-437`。D 口 80% 自由滑行/主动刹车后续计数为 `136/13`。
- 通用控制按产品需求不设置固件自动超时；输出保持到上位机明确改变状态。升级开始和关机路径仍会关闭全部输出。

- A 设备已由 `M0.34A` 自升级，通过官方 Bootloader 搬运并启动 `M0.35A`；屏幕版本显示、蓝灯闪烁和 USB 心跳均正常。
- 输出 A 口已沿用旧固件接线 `PB9/TIM4_CH4 + PG10/PG11` 完成实机验证：上电不自动转；收到电脑命令后低速正转、暂停、反转并停止；升级和关机路径会强制停转。
- 输出 A 口测速已沿用 `PE5/TACHOA0 + PE6/TACHOA1` 完成实机验证：正转计数 `+165`，反转计数 `-168`，方向相反且数量接近，证明马达内部反馈链路正常。
- 输出 A 口 100% 功率短测试已完成：正转计数 `+421`、反转计数 `-375`，方向正确并最终关闭驱动；测试总计数 `+568` 说明中间关闭驱动时仍有明显惯性滑行。
- 当前 Bootloader 搬运进度到 `100%` 后不会自动清屏或重新开机；灯熄灭后需要人工断电重启。这一现象本身不代表升级失败。
- 外部 Flash 实物为 Winbond `W25Q256JV`，JEDEC ID 为 `EF 40 19`，容量 32 MiB。
- 已在空白地址 `0x01FFF000` 完成临时 32 字节写入、读回、低地址防串写、扇区擦除和空白复查，全部成功。
- 已确认有效方案是：`0xB7` 进入 4 字节模式，继续使用原 `0x03/0x02/0x20` 指令并发送 4 个地址字节，完成后用 `0xE9` 恢复 3 字节模式。
- FATFS 前 28 MiB 的正确容量为 `7168` 个 4 KiB 扇区，旧值 `2048*28` 错误地表示了 224 MiB。
- Git 可追踪的官方源码修正工具位于 `firmware/official_est3_app/tools/apply_w25q256_fix.py`。当前电脑没有 Keil ARMCC，暂时不能重新链接完整官方 APP；原工程要求 ARMCC `V5.06 update 4 (build 422)`。

## 1. 项目目标

本项目是在不修改板内 Bootloader 的前提下，重构 EST 主控下位机固件。

当前阶段的第一目标不是完整恢复所有业务功能，而是先保住升级链路：

1. 确认当前板子的 Bootloader、Flash 布局和 USB HID 升级协议。
2. 准备一个能通过当前 Bootloader 正确搬运并启动的 APP。
3. 确保新 APP 启动后仍能通过 USB HID 接收下一版升级包。
4. 在升级闭环可靠后，再逐步重构硬件驱动和业务功能。

换句话说，当前工作的验收标准是“APP 能启动，并且能继续升级”，不是“所有硬件功能一次性完成”。

## 2. 当前权威资料

当前板子应以用户提供的以下资料为准：

```text
H:\EST第二期\EST 3.0
```

已确认：

- 板上 Bootloader 心跳版本为 `03.00B`。
- 该版本与 `EST 3.0` 资料中的 Bootloader 版本匹配。
- 当前升级包头为 4 字节 ASCII `APP=`。
- 当前 APP 起点为 `0x08010000`。
- Update 包起点为 `0x08080000`。

旧迁移文档正文中关于 2018 旧 Bootloader、`0x0800C000` 作为 APP 起点、以及 `EST` 三字节包头的判断，均只作为历史过程保留，不再作为当前执行依据。

## 3. 当前 Flash / 升级布局

当前采用的关键地址：

| 地址 | 用途 |
| --- | --- |
| `0x08000000` | Bootloader 区 |
| `0x08010000` | APP 起始地址 |
| `0x08080000` | Update 包写入起点 |
| `0x08080004` | Bootloader 跳过 `APP=` 后读取 APP 数据的位置 |
| `0x0800C000` | 升级长度低 32 位 |
| `0x0800C008` | 升级长度高 32 位 |
| `0x0800C010` | 升级 pending 状态 |

注意：`0x0800C000` 当前不是 APP 起点，而是升级元数据区域的一部分。

## 4. USB HID 升级入口

当前板子 USB HID 升级入口已确认可见：

| 项 | 当前值 |
| --- | --- |
| VID/PID | `0483:5750` |
| Bootloader 心跳版本 | `03.00B` |
| HID report 输入/输出长度 | `1025` |
| Device 控制器 | `USB OTG HS + USB3300 ULPI` |
| HID report 有效长度 | `1024` 字节；Windows API 另含 1 字节 Report ID 占位 |
| 当前发送工具 | `tools/est_hid_sender/est_hid_sender.py` |

常用命令：

```powershell
python tools\est_hid_sender\est_hid_sender.py ping
python tools\est_hid_sender\est_hid_sender.py verify --file "固件.upgrade.bin"
python tools\est_hid_sender\est_hid_sender.py flash --file "固件.upgrade.bin"
```

发送器对第 0 包使用 15 秒 ACK 超时，对其余包使用 4 秒 ACK 超时；发送前默认校验 manifest 并显示当前/目标版本，每次升级保存日志。

## 5. 当前已有产物

### 官方恢复包

当前工作区已经从 `EST 3.0` 官方 APP 包装出恢复包：

```text
firmware\official_est3_app\build\EST_Main_V3_official.upgrade.bin
```

manifest 信息：

| 字段 | 值 |
| --- | --- |
| version | `03.02A` |
| raw app size | `411908` |
| unpadded package size | `411912` |
| upgrade package size | `411912` |
| bootloader stored length | `411911` |
| initial MSP | `0x2002D3B0` |
| reset handler | `0x0801019D` |
| header | `APP=` |
| sha256 | `df358dedd80a77d6db76d2b3852ba67be2b20019659799b3296a42b645df0914` |

如果当前目标是让板子恢复到官方 APP，应优先围绕这个恢复包操作。

### 最小 APP 工程

当前最小 APP 工程位于：

```text
firmware\minimal_upgrade_app
```

该工程目标是实现最小可升级 APP，内容包括：

- 启动后保持电源控制脚。
- 清理 Bootloader 遗留中断和外设状态。
- 配置 APP 向量表和系统时钟。
- 枚举 USB HID。
- 支持心跳和版本返回。
- 接收升级包并写入 Update 区。
- 写入升级长度和 pending 标志。
- 复位交给 Bootloader 搬运。
- 以独立驱动实现输出 A 口的安全正反转诊断，不依赖旧 APP 主体。

当前工程文件已按 `0x08010000` APP 起点更新，关键文件包括：

- `firmware\minimal_upgrade_app\include\app_config.h`
- `firmware\minimal_upgrade_app\memory.ld`
- `firmware\minimal_upgrade_app\tools\package_firmware.py`
- `firmware\minimal_upgrade_app\tools\verify_build.py`

## 6. 已失败或废弃的最小 APP 版本

以下版本不要继续发送：

| 版本 | 状态 |
| --- | --- |
| `M0.08A` | 已通过 HID 完整发送，`260/260` ACK 且 `done`，但启动后无红/蓝灯闪烁，USB 未枚举，视为启动失败 |
| `M0.09A` | 基于错误的 `EST` 包头判断生成，已被 `EST 3.0` 结论推翻，废弃 |
| `M0.10A` | 按 `APP= + 0x08010000` 生成并成功发送，但心跳无响应；复盘确认仍误用了 FS/64 字节 HID，且把 `PE2` 拉低，启动失败 |

当前候选版本为 `M0.11A`：已改为 EST 3.0 官方的 HS ULPI/1024 字节 HID，并按官方 `Power_On()` 将 `PE2` 保持为高电平。本机构建、产物校验和 9 项测试均已通过。实机已完成 `260/260` 发送并返回 `done`，但跳转后的首次及重复心跳均无响应，后续写入出现 timeout。当前需要通过物理 USB 断开重连和 LED 状态区分“APP 未运行”与“HS PHY 未产生完整重新枚举”。

候选包：

```text
firmware\minimal_upgrade_app\build\v11_hs_ulpi\est_minimal_upgrade_app.upgrade.bin
```

SHA-256：`45800f830bf2d00910ed3ed80cd66e8b47de3c35969803dd6008ec265a5c987f`

## 7. 我们当前实际工作内容

当前我们的工作不是改 Bootloader，也不是继续依赖旧上位机手动选文件。

当前工作内容明确为：

1. 整理并统一 EST 3.0 资料、当前板子行为和工作区代码。
2. 维护一个可由当前 Bootloader 接收并搬运的升级包格式。
3. 使用 `tools/est_hid_sender` 作为主要发送工具。
4. 在需要恢复板子时，优先使用官方恢复包。
5. 在继续开发时，基于 `minimal_upgrade_app` 生成新版本最小 APP。
6. 逐步验证：发送成功、Bootloader 搬运成功、APP 启动成功、USB 再枚举成功、下一版升级成功。

后续硬件驱动重构应在升级闭环稳定后进行，顺序建议为：

1. GPIO、电源保持、LED、按键。
2. LCD 基础显示。
3. 外部 Flash `W25Q256JV`（JEDEC ID `EF 40 19`，32 MiB，实物确认）。
4. 音频 `VS1053B`。
5. 蓝牙/串口。
6. 4 路输入端口。
7. 4 路电机输出、测速和电流检测（A 口正反转和测速已完成；其余端口及电流检测待完成）。

## 8. 当前下一步可执行路径

根据目标不同，下一步分两条：

### 路径 A：先恢复官方 APP

目标：让板子回到 `EST 3.0` 官方 APP 状态。

执行对象：

```text
firmware\official_est3_app\build\EST_Main_V3_official.upgrade.bin
```

建议步骤：

1. 先执行 `ping`，确认 Bootloader 仍返回 `03.00B`。
2. 使用 HID sender 发送官方恢复包。
3. 等待发送完成和设备复位。
4. 观察 USB 是否重新枚举，确认 APP 是否恢复。

### 路径 B：继续开发最小 APP

目标：生成新的最小可升级 APP，替代已失败的 `M0.08A` 和废弃的 `M0.09A`。

建议步骤：

1. 检查当前 ARM GCC 和 make 环境是否可用。
2. 确认 `minimal_upgrade_app` 中 APP 起点、包头、升级元数据地址均为 EST 3.0 规则。
3. 使用已生成并验证的 `M0.11A` 升级包。
4. 发送前检查 manifest、包头、向量表和 reset handler。
5. 发送后观察 LED、USB 枚举和心跳。
6. 若 `M0.11A` 能启动，再生成只改变版本号的下一版，验证自升级闭环。

## 9. 明确不做的事

当前阶段不做以下事项：

- 不修改板内 Bootloader。
- 不再把 `0x0800C000` 当作 APP 起点。
- 不再使用 `EST` 三字节包头作为当前升级包格式。
- 不再发送 `M0.09A`。
- 不再发送已确认失败的 `M0.10A`。
- 不把 2018 旧 Bootloader 当作当前权威资料。
- 不优先依赖旧上位机手动选择固件文件。
- 不在升级闭环未恢复前展开大规模业务驱动重构。

## 10. 关键判断

当前项目的关键判断是：

```text
先恢复或重建“可继续升级的 APP”，再重构完整硬件功能。
```

只要新 APP 启动后 USB 不枚举，就说明升级链路没有保住；即使 HID 发送过程显示 ACK 全部成功，也不能算最终成功。真正成功必须同时满足：

1. 固件包发送完成。
2. Bootloader 搬运后 APP 能启动。
3. APP 能重新枚举 USB HID。
4. APP 能响应心跳。
5. APP 能继续接收下一版升级包。
