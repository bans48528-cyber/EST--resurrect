# EST C API 与 MicroPython 下一步工作清单

更新时间：2026-08-28

## 0. 当前进度

- M0.73A 已建立 `est_types.h`、`est_motor.h` 和 `est_drive.h`，冻结公共错误码、A-D/1-4 端口、停止方式、马达/传感器类型与首版单电机、底盘函数签名。
- `est_motor.c` 已把开环功率、持续定速、相对角度、停止、全部停止、角度清零、类型和状态查询接到已验证的 `board_motor_*`。现有 HID 功率、位置和定速入口已改经 `est_motor_*`，协议字节格式不变。
- M0.73A 已在 C 设备完成实机回归：开环 10% 点动、相对 `+60°` 精确到位和 30% 持续定速均通过，停止后四路输出功率为 0，证明现有 HID 经服务层调用的行为与 M0.72A 基线一致。
- M0.74A 已将速度/位置闭环扩展为 A-D 每端口独立状态，并改为逐端口启动、停止、查询和 10 ms 控制更新。C 设备的 C/D 并发定速、单路停止不影响另一路和并发 `+180°/-180°` 均已通过；57 项固件测试、86 项 HID 测试、ARM `-Werror`、144 引脚检查和包校验也已通过。
- M0.75A 已实现首版 `est_motor_pair_run_angles()`、`est_drive_stop()` 和 `est_drive_get_status()`，加入双路编码器进度差修正及联动故障停止。A/C 双大型马达反向一圈最大误差 `5°`，同向 20% 五圈最大误差 `7°`；持续压住 C 可触发联动超时并安全自由滑行。58 项固件测试、89 项 HID 测试、ARM `-Werror` 和包校验通过。
- M0.76A 按产品要求取消双电机同步的自动时间上限；单电机超时不变。无外力十圈超过旧上限仍完成，对 A 施加外力后松开也能继续追赶并完成，电脑端不再设置 18 秒截止时间。58 项固件测试、90 项 HID 测试、ARM `-Werror` 和包校验通过。
- M0.83A 已实现一组双电机持续闭环定速、显式滑行/刹车停止和实时状态查询。A/C 双大型在同向 20%、40%、80% 及反向 40% 下完成实机验收；40% 受力跟随和 80% 主动刹车同时停止也已确认。59 项固件测试、93 项 HID 测试、ARM `-Werror` 和包校验通过。
- M0.84A 已实现并实机验证按毫米直行的轮径换算链路，但按产品要求只保留为诊断功能，不进入最终软件界面。
- M0.85A 已实现 `est_drive_run_degrees()`、`est_drive_run_time()` 和统一状态查询，HID `drive-run` 支持圈、度、秒三选一。A/C 双大型的 `+720°`、反向 `-2.5` 圈和 60% 定时 3 秒均已实机通过；定时停止由 C 固件内部完成。61 项固件测试、99 项 HID 测试、ARM `-Werror` 和包校验通过。
- M0.86A 已把 `est_motor_pair_run_angles()` 扩展为不同行程比例同步。A/C 双大型的同向和反向 2:1 目标均通过实机验收，长短行程正确且基本同时停止；同步误差统一为投影到较长目标后的等效角度差。
- M0.87A 已完成实机验收：`est_motor_pair_run_speeds()` 和 HID `0x1E` 支持两路独立的非零 `10..100%` 有符号速度，进度、同伴限速和纠偏量均按目标速度比例投影。A/C 双大型的 `40%/20%` 与 `80%/40%` 均稳定保持约 `2:1`，最大等效误差分别为 `18°/25°`；协议为 `1.11`。
- M0.88A 已完成基础实机验收。`est_drive_start_steer()`、HID `0x21` 和 `drive-steer` 按 EV3 Classroom 公式把转向值与有符号移动速度换算为左右独立速度；A/C 双大型的速度 80、转向 `+50` 得到 `80%/40%`，最终 `2230°/1120°`、比例约 `1.99:1`、最大等效误差 `16°`，并安全停止。协议为 `1.12`，62 项固件测试、102 项 HID 测试、ARM `-Werror` 和严格包校验均通过。
- M0.90A 已实现 `est_drive_steer_for()`、HID `0x22` 和 `drive-steer-for`。它复用同一转向公式，可按外侧轮圈数/角度生成左右比例目标，也可由固件内部按秒计时自动停止；协议升为 `1.13`。64 项固件测试、105 项 HID 测试、ARM `-Werror` 和包校验通过；圈数、角度、秒数、正反向、`±100` 原地旋转和主动刹车均已完成锂电实机验收。
- M0.91A 已建立 `est_sensor.h/.c`，把现有 `board_sensor_*` 包装为统一的类型、状态、模式、值格式、模式切换、端口重启和状态查询接口。温度与陀螺仪值在 C API 中按有符号数解释，同时保留原始 16 位值和原始类型字节供旧 USB 协议原样返回；协议仍为 `1.13`。65 项固件测试、105 项 HID 测试、144 引脚检查、ARM `-Werror` 和包校验通过，传感器识别、实时读数和模式切换也已完成实机回归。
- M0.92A 已建立 `est_display.h`、`est_buttons.h`、`est_led.h` 和 `est_backlight.h`。屏幕、版本文字、蓝灯、背光、电量和 USB 普通回归均已在实机通过，USB 帧与协议 `1.13` 不变。
- M0.93A 已建立 `est_battery.h/.c` 和 `est_system.h/.c`。电池状态统一包含有效性、档位、百分比、低电量、ADC 原值和分压采样值；系统接口统一初始化、毫秒时基、紧急停止、退出清理和关机。未经旧引导验证的软件重启明确返回 `EST_ERR_NOT_SUPPORTED`。67 项固件测试、105 项 HID 测试、144 引脚检查、ARM `-Werror` 和包校验通过；USB、电池、四路传感器、屏幕、蓝灯和背光实机回归通过。
- M0.94A 已完成 MicroPython `v1.29.0` 最小运行时实机验收。`0x23` 返回 `state=passed`、48 KiB 堆剩余 45728 字节、启动 10 ms、最大 GC 停顿 593 us、自检值 96；屏幕、USB、四路传感器和全部马达停止状态正常。
- M0.95A 新增协议 `1.15` 的 RAM Python 程序命令 `0x24`。最多 8 KiB 源码按顺序分块并核对 CRC32；实机计算结果、Python 异常和 300 ms 硬超时通过。该版暴露出 VM 运行期间 USB 主循环停顿、主动停止无法进入设备的问题。
- M0.96A 保持协议字节不变，在 VM 中每 1 ms 轮询 USB，并在脚本运行期间只允许 `0x24` 状态/停止。实机 10 秒死循环在约 1 秒主动停止，最终清理状态、四路马达停止、传感器重新同步及再次运行脚本均通过。当前 RAM 脚本只能做流程和最小服务层验证，尚不能启动马达。
- M0.97A 建立主循环和 VM hook 共用的 1 ms `est_runtime_tick()`，统一维护电机闭环、传感器、按键、电池、背光、音频和协议超时。1 秒 Python 忙循环实测 tick 增加 1001 次，红外 UART 接收计数从 3285 增至 6030；计算、300 ms 超时、显式停止和清理后恢复均通过。LCD 页面刷新仍为主循环任务。
- M0.98A 新增正式只读 `est.Sensor(1..4)`、`est.buttons` 和 `est.battery`。四路已接传感器类型/状态/值、六键掩码与单键查询、电池百分比/档位/低电量以及非法端口异常均完成实机验收；统一 tick 回归保持 1001 次/秒。
- M0.99A 新增正式 `est.Motor("A".."D")` 只读状态对象和逐端口安全停止。类型、状态、停止方式、功率、目标/实际速度、角度、错误及 8 项状态元组均包装 `est_motor_*`；A/C 大型、B 空、D 中型以及静止条件下四路 `BRAKE -> COAST` 已完成实机验收，脚本结束后四路功率为 0。
- M1.00A 为 `Motor` 新增 `run_power()`、`run_speed()`、`run_angle()` 和 `reset_angle()`，并将参数错误、未连接、类型不匹配、忙和不支持映射为明确 Python 异常。A 口大型马达已通过强制 GC 下的持续闭环和 `+90°`，D 口中型马达已通过 `-90°`；正常结束、异常、硬超时和主动停止后 A-D 全部 coast/power 0。
- M1.01A 实现 `est_motor_run_time()` 和 `Motor.run_time()`。四个端口各自保存开始时间、持续时间和完成停止方式，由 C 服务 tick 自动结束；A 大型、D 中型、强制 GC、coast、brake、中途取消和异常清理均已实机通过。
- M1.02A 开放 `MotorPair` 和 `DriveBase`，同型号双电机的持续、定时、角度、直行、转向、对象停止和退出清理均已实机通过。
- M1.03A 首次开放类型化传感器，但把陀螺仪端口通信重启误作角度归零，稳定在测试阶段 32 超时；该版已废弃。
- M1.04A 修正为对象内陀螺仪软件零点，并为模式读取增加 500 ms 重试和最多 3000 ms 的新数据等待。红外、声音、陀螺仪和超声波三轮真机脚本均通过；颜色、触摸和温度类已实现，等待对应实物回归。
- M1.05A 开放 `est.display`、`est.led` 和 `est.backlight`。整帧 LCD 刷新按小传输块执行 VM hook；测试图、红/蓝/红蓝灯和背光 20%/0%/100% 三轮真机通过，脚本结束后状态页恢复。
- `EST_STOP_HOLD` 仍明确返回 `EST_ERR_NOT_SUPPORTED`，不会静默降级。
- 已对照 EV3 Classroom 1.5.2：移动积木使用转向值 `-100～100` 或左右独立速度，不提供按轮径、轮距和机身角度换算的独立转向积木。M0.88A 已实现持续转向，M0.90A 已实现按圈/度/秒完成一次转向；原地旋转由转向值 `±100` 表达。

## 1. 目标

在现有 `board_*` 硬件驱动之上建立稳定的 `est_*` C 服务层，再由 MicroPython 和后续积木程序调用。实时控制、安全保护和硬件差异留在 C；用户流程、条件判断和动作组合放在 MicroPython。

本清单用于下一阶段开发，不表示下面的接口目前已经全部实现。

## 2. 已确定的架构

```text
STM32 外设和中断
        ↓
board_* 硬件驱动
        ↓
est_* C 服务 API
   ↙              ↘
USB/HID        MicroPython est 模块
                    ↓
               用户程序/积木
```

必须遵守以下边界：

- 电机 PWM、编码器、速度闭环、位置闭环、双电机同步、看门狗、堵转保护和紧急停止在 C 中执行。
- MicroPython 负责动作组合、流程、条件、循环和普通数值比较。
- 灰度 `< 30` 等用户阈值在 MicroPython 或积木程序中处理。
- 触碰识别电压、滤波、防抖等硬件阈值在 C 中处理。
- Python 脚本异常、软重启或垃圾回收不能中断 C 的安全控制任务。
- C 层不能依赖 Python 正常运行才能停止电机。

## 3. 参数与单位

| 参数 | V1 约定 | 归属 |
|---|---|---|
| 电机功率 | `-100～100%`，表示 PWM 驱动力 | C 校验 |
| 电机速度 | `-100～100%`，表示对应电机类型的目标速度 | C 闭环 |
| 电机角度 | 度，当前单次限制 `-3600～3600°` | C 校验与控制 |
| 电机圈数 | `1 圈 = 360°` | MicroPython 换算 |
| 运行时间 | 毫秒 | C 计时 |
| 距离 | 毫米，仅保留诊断接口，不进入最终软件界面 | C API |
| 温度 | `0.1°C` | C API |
| 灰度/环境光/声音 | `0～100` | C 输出，Python 比较 |
| 屏幕 | `180×128` 单色像素 | C 绘图 |
| 电池电压 | 毫伏 | C API |

电机功率和速度不能混用：功率是固定驱动力，不保证实际转速；速度是闭环目标，C 会根据编码器反馈自动调整功率。

## 4. 公共类型和错误码

- [x] 建立 `est_types.h`。
- [x] 定义电机端口 `A-D` 和传感器端口 `1-4`。
- [x] 定义 `EST_OK`、参数错误、端口错误、未连接、类型不匹配、不支持、忙、超时、I/O 错误和状态错误。
- [x] 定义停止方式：自由滑行、主动刹车、位置保持。
- [x] 定义电机类型：无、大型、中型、未知。
- [x] 定义传感器类型、模式、连接状态和数值格式。
- [x] 所有可能失败的读取接口使用“状态返回值 + 输出指针”，不使用特殊负数同时表达错误和值。

## 5. C API 模块清单

### 5.1 系统 `est_system.h`

- [x] `est_system_init()`：初始化电源保持和系统时基（M0.93A）。
- [ ] `est_tick(now_ms)`：执行非中断周期任务。
- [x] `est_system_millis()`：读取系统毫秒数（M0.93A）。
- [x] `est_system_emergency_stop()`：立即安全停止全部马达并关闭传感器输出（M0.93A）。
- [ ] `est_system_reboot()`：接口已声明，但在验证旧引导安全路径前返回不支持。
- [x] `est_system_power_off()`：先清理输出再安全关机（M0.93A）。
- [x] `est_system_cleanup()`：Python 退出、异常和后续软重启共用的清理入口（M0.93A）。

### 5.2 单电机 `est_motor.h`

- [x] `est_motor_get_type()`：读取大型/中型/无电机状态。
- [x] `est_motor_set_power()`：开环功率控制。
- [x] `est_motor_run_speed()`：持续速度闭环。
- [x] `est_motor_run_time()`：按时间运行，计时由 C 完成（M1.01A；大型/中型、滑行/刹车、取消/异常通过）。
- [x] `est_motor_run_angle()`：按相对角度运行；当前完成后回到自由滑行。
- [x] `est_motor_stop()`：滑行和刹车已接入；位置保持尚未实现。
- [x] `est_motor_stop_all()`：同时滑行停止或主动刹车全部电机。
- [x] `est_motor_reset_angle()`：累计角度清零。
- [x] `est_motor_get_status()`：读取类型、功率、目标速度、实际速度、角度、状态和错误。
- [ ] 将大型和中型电机的测速、最低速度、减速和控制增益分成独立 profile。
- [x] 每个端口使用独立控制状态，允许多电机并发（M0.74A 实机通过）。

### 5.3 精确双电机/底盘 `est_drive.h`

- [x] `est_drive_config()`：设置左右端口、轮径和轮距。
- [x] `est_motor_pair_run_angles()`：原子启动两个电机，支持相同或不同非零目标的比例同步（M0.86A 实机通过）。
- [x] `est_motor_pair_run_speeds()`：一组双电机持续闭环定速；M0.87A 已实机验证两路独立非零速度的比例同步，固件不设自动超时。
- [x] `est_motor_pair_stop()` 和持续定速状态查询：显式自由滑行/主动刹车及速度、PWM、角度、同步误差回报。
- [x] `est_drive_straight()`：按毫米直行；M0.84A 实机通过，仅保留为诊断接口。
- [x] `est_drive_run_degrees()`：按有符号角度直行；圈数由调用端按 `1 圈 = 360°` 换算。
- [x] `est_drive_run_time()`：按有符号时间直行；由固件内部计时并自动滑行或刹车停止。
- [x] `est_drive_get_motion_status()`：统一读取角度/圈数/时间任务的进度、两轮角度和同步误差。
- [x] 扩展 `est_motor_pair_run_speeds()`：支持两路不等的独立有符号速度，对应 EV3 `drive_start_dual_speed`（M0.87A 实机通过）。
- [x] `est_drive_start_steer()`：按 EV3 转向值公式换算左右速度并持续移动；`±100` 自然得到原地旋转（M0.88A 的 `+50 / 80%` 已实机通过）。
- [x] `est_drive_steer_for()`：复用 M0.86A 比例行程同步，覆盖圈、度、秒三种数量单位（M0.90A 实机通过）。
- [x] `est_drive_stop()`：同时停止底盘。
- [x] `est_drive_get_status()`：读取轨迹进度、左右误差和故障。
- [x] 实现左右电机各自的速度闭环，并在持续定速任务中按编码器进度协调两路速度。
- [x] 实现基于编码器角度差的首版交叉同步修正。
- [ ] 实现加速、匀速、减速轨迹，减少起停打滑。
- [x] 双电机同步不设自动时间上限；受阻后松开继续运行，显式停止、升级和关机仍同时停止两路。
- [ ] 支持编码器基础模式和“编码器 + 陀螺仪航向修正”精确模式。

说明：编码器可以保证轮子转动精确；机器人在地面的实际轨迹还会受到轮胎打滑、轮径差和地面影响。要求高精度直行与转向时，应加入陀螺仪航向反馈。

### 5.4 传感器 `est_sensor.h`

- [x] 通用类型、状态、模式读取（M0.91A）。
- [x] 通用模式设置和端口重启（M0.91A）。
- [x] 颜色/灰度：反射光、环境光、颜色编号。
- [x] 触碰：按下状态。
- [x] 超声波：毫米距离、目标存在状态。
- [x] 陀螺仪：角度、角速度。
- [x] 声音：声音强度。
- [x] 温度：摄氏/华氏温度的有符号十分之一度数值。
- [x] 红外：接近距离、信标方向/距离、遥控按键。
- [x] 统一无效值、陈旧数据、断线和模式切换期间的状态/错误语义。
- [ ] 不增加“大于、小于、等于”等用户比较接口。

### 5.5 屏幕和输入

- [x] `est_display.h`：清屏、像素、直线、矩形、英文文字、1 位位图和刷新（M0.92A）。
- [x] 帧缓冲由 `board_lcd` 独占；绘图调用只修改内存，调用 `est_display_refresh()` 时发送完整 `180×128` 帧，首版不支持局部刷新。
- [x] `est_backlight.h`：设置和读取 `0..100%` 背光（M0.92A）。
- [x] `est_buttons.h`：当前状态、按下、松开和一次性长按事件（M0.92A，长按阈值 1000 ms）。
- [x] `est_led.h`：红灯、蓝灯、红蓝组合和关闭（M0.92A）。

### 5.6 音频、电池和电源

- [ ] `est_audio.h`：音调、音量、停止、播放文件、播放状态。
- [ ] 音频与 W25Q256 共用 SPI3 时由 C 统一仲裁。
- [x] `est_battery.h`：ADC 原值、分压采样值、电量档位、百分比和低电量状态（M0.93A）。
- [ ] 定义低电量时限制电机输出及安全关机策略。

### 5.7 存储、通信和配置

- [ ] `est_storage.h`：挂载、打开、读、写、关闭、删除、目录和剩余空间。
- [ ] 在 W25Q256 上确定文件系统和分区布局。
- [ ] 用户程序、声音和资源与系统数据分区隔离。
- [ ] `est_comm.h`：USB/蓝牙用户字节流；Bootloader 升级协议保持系统内部使用。
- [ ] `est_config.h`：轮径、轮距、音量、传感器校准和用户阈值的持久化。

## 6. MicroPython 接口映射

- [x] `Motor`：状态、停止、开环功率、闭环速度、按时间、相对角度和角度清零均已调用单电机 C API（M0.99A-M1.01A）。
- [x] `MotorPair`/`DriveBase`：M1.02A 已调用精确双电机 C API，并完成动作、对象停止、异常清理和电脑主动停止实机验收。
- [x] 通用 `Sensor` 对象：端口、类型、状态、模式、值格式、有效性、错误和值（M0.98A），以及 `set_mode()`、`read_mode()`、`restart()`（M1.04A）。
- [x] 七种类型化传感器类：声音、温度、触摸、颜色、超声波、陀螺仪和红外均已实现类型检查与专用方法；当前连接的红外、声音、陀螺仪和超声波已完成实机回归（M1.04A）。
- [x] 只读 `Buttons` 和 `Battery`（M0.98A），以及 `display`、`led` 和 `backlight` 单例（M1.05A）；`Audio` 和 `Storage` 仍待实现。
- [x] M1.00A 已确认首批 `Motor` 运行方法在 Python 入口和 C 服务层均检查参数范围；后续新增接口继续遵守同一规则。
- [x] M1.02A 的双电机 `wait=True` 通过轮询 C 状态等待完成，并持续调用统一 VM hook；没有用 Python `sleep` 代替 C 计时。

目标调用示例：

```python
motor = Motor("A")
motor.run_time(speed=50, time=1000, stop=Stop.BRAKE)

pair = MotorPair("A", "C")
pair.run_angle(left_degrees=360, right_degrees=360, speed=50, wait=True)

drive = DriveBase("A", "C")
drive.straight_angle(degrees=360, speed=50, wait=True)
drive.steer_angle(steering=50, degrees=360, speed=30, wait=True)

gray = est.ColorSensor(1)
if gray.reflection() < 30:
    drive.stop()
```

## 7. 实施顺序

### 阶段 A：冻结契约

- [ ] 建立头文件目录和公共命名规范。
- [ ] 冻结错误码、单位、范围、状态机和并发规则。
- [ ] 明确新命令覆盖旧命令还是返回 `EST_ERR_BUSY`。
- [ ] 明确所有 Python 退出路径的电机安全行为。

### 阶段 B：适配现有驱动

- [x] 用 `est_system`、`est_motor`、`est_sensor` 和已完成的外围服务包装现有 `board_*`。
- [x] `est_motor`、`est_sensor`、屏幕、按键、LED、背光、电池和系统已包装现有驱动。
- [x] 不复制已验证的寄存器驱动和传感器协议代码。
- [x] 为现有适配层增加主机测试和固件构建检查。

### 阶段 C：单电机并发控制

- [x] 将当前单实例速度/位置状态扩展为每端口独立状态（M0.74A 实机通过）。
- [x] 完成角度、停止和状态查询；M1.00A 已从 MicroPython 实机调用。
- [x] 完成单马达按时间运行（M1.01A）。
- [ ] 验证 A-D 端口并发、取消、超时和急停。

### 阶段 D：精确双电机轨迹

- [x] 完成相同绝对角度及不同行程比例同步（M0.75A、M0.86A）。
- [x] 加入角度差修正。
- [ ] 加入加减速规划。
- [ ] 使用大型和中型电机分别标定。
- [ ] 加入可选陀螺仪航向修正。
- [ ] 在固定电量、轮胎和测试场地上记录重复性数据。

初始验收建议：

- [x] 两个电机使用同一时基原子启动，时间差不超过一个控制周期。
- [x] 同步执行期间左右编码器误差有记录且能收敛。
- [ ] 连续执行至少 10 次，均能正常到位并安全停止。
- [x] 单侧受阻超过旧时间上限后不进入自动故障；松开后继续追赶并完成。永久受阻由用户显式停止，主动断开仍待单独测试。
- [ ] 实际直行和转向误差先实测建立基线，再冻结量化指标。

### 阶段 E：MicroPython 最小验证

- [x] 精简 MicroPython 并链接到 `0x08010000` APP；M0.94A 已完成实机启动与自检验收。
- [x] 建立最小内建 `est` 模块，先绑定系统时间、马达类型/全部停止、底盘转向换算和传感器状态；完整类接口后续扩展。
- [x] 测量固件大小、启动时间、剩余 Python 堆和 GC 停顿；M0.94A 实机指标已由 `0x23` 读回。
- [x] 建立最多 8 KiB 的 RAM 源码上传、CRC32、运行、状态、主动停止和 100-10000 ms 硬超时链路（M0.96A）。
- [x] 脚本正常结束、Python 异常、主动停止和硬超时均在实机验证全部马达停止及传感器恢复。
- [x] 建立 Python 运行期间的统一 C 服务 tick，使电机闭环、传感器、按键、电池、背光、音频和协议超时不依赖 Python 主动让出（M0.97A 实机通过）；M1.05A 进一步让 MicroPython 整帧 LCD 刷新按小传输块执行同一 VM hook。
- [x] 建立正式只读 `Sensor`、`Buttons` 和 `Battery` Python API，并完成四路当前传感器和锂电池实机验收（M0.98A）。
- [x] 建立正式 `Motor` 只读状态和逐端口 `COAST/BRAKE` 安全停止 API（M0.99A；真实大型、中型及空口通过）。
- [x] 开放 `Motor.run_power()`、`run_speed()`、`run_angle()` 和 `reset_angle()`（M1.00A；真实大型和中型马达通过）。
- [x] 开放 C 层计时的 `Motor.run_time()`，并验证自动滑行、自动刹车、中途取消和异常清理（M1.01A）。
- [x] 强制 GC 下验证单马达闭环持续运行、角度到位和安全停止（M1.00A）。
- [x] M1.02A 已验证双电机同步、同型号限制、强制 GC、`wait=True`、完成状态、对象停止、电脑主动停止和异常清理。

### 阶段 F：外围完整化

- [x] 屏幕基础绘图 C API（M0.92A；中文字体、图片资源管理和局部刷新尚未实现）。
- [x] 类型化传感器、模式读取、端口重启与陀螺仪软件归零（M1.04A；颜色、触摸和温度等待对应实物回归）。
- [x] MicroPython 屏幕绘图、显式刷新、红蓝双色灯和 `0..100%` 背光（M1.05A 实机通过）。
- [ ] 文件系统和用户程序保存。
- [ ] 音频播放。
- [ ] 用户配置。
- [ ] USB/蓝牙用户通信。
- [ ] 积木到 MicroPython 代码生成。

## 8. 下一步首先执行

1. [x] 建立 `est_types.h`，确定公共错误码、端口、停止方式和状态。
2. [x] 建立 `est_motor.h` 和 `est_drive.h`，先冻结函数签名及行为。
3. [x] 把当前功率、速度、角度、停止、清零和状态 `board_motor_*` 接入单电机 `est_motor_*` 适配层。
4. [x] 将电机控制状态改为 A-D 每端口独立，为双电机同步打基础（M0.74A 实机通过）。
5. [x] 完成双电机相同角度同步的第一版 C 控制器并进行实机测试（M0.75A）。
6. [x] 完成按毫米直行诊断，以及最终产品使用的圈数、角度和秒数直行（M0.84A-M0.85A）。
7. [x] 完成左右轮不同角度比例同步（M0.86A）。
8. [x] 支持左右独立持续速度（M0.87A）。
9. [x] 按 EV3 Classroom 的转向值公式实现持续转向（M0.88A 基础实机通过）。
10. [x] 复用比例行程同步实现圈数、角度和秒数定量转向（M0.90A，锂电实机验收通过）。
11. [x] 建立统一 `est_sensor` C API，并让现有 USB 传感器入口改经服务层（M0.91A，实机回归通过）。
12. [x] 建立屏幕、按键、LED 和背光的公共 C API（M0.92A，实机回归通过）。
13. [x] 建立电池和系统公共 C API，统一电量、时间、急停、清理和关机入口（M0.93A；软件重启待确认旧引导安全路径）。
14. [x] 完成 M0.94A MicroPython 最小运行时实机启动、`PY:OK`、堆/GC 状态和普通硬件回归。
15. [x] 完成 M0.96A RAM Python 源码上传、CRC32、执行结果、异常、硬超时和主动停止实机验证。
16. [x] 建立 Python 运行期间的统一 C 服务 tick（M0.97A；1 秒忙循环获得 1001 次 tick，UART 持续接收）。
17. [x] 开放正式只读 `Sensor`、`Buttons`、`Battery` API（M0.98A，四路当前传感器实机通过）。
18. [x] 建立 `Motor` 的只读状态与安全停止（M0.99A，A/C 大型、B 空、D 中型实机通过）。
19. [x] 开放单马达功率、闭环速度和相对角度运行，并完成真实大型/中型马达、强制 GC、异常、超时和主动停止回归（M1.00A）。
20. [x] 补齐单马达按时间运行及停止动作语义（M1.01A；A 大型和 D 中型实机通过）。
21. [x] M1.02A 已开放 `MotorPair`/底盘 MicroPython API，并通过动作、对象停止、电脑主动停止和异常清理实机验收。
22. [x] M1.04A 已开放七种类型化传感器、模式操作和端口重启；红外、声音、陀螺仪和超声波实机通过，陀螺仪采用对象内软件归零。
23. [x] M1.05A 已开放 `display`、`led` 和 `backlight`，并通过测试图、三种灯态、三档背光及刷屏 VM hook 实机验收。

M1.05A 是当前实机基线。下一步设计用户程序持久化、存储分区和开机启动策略；音频仍作为独立未通过问题，不直接开放用户 API。毫米直行诊断流程不直接作为最终用户 API。
