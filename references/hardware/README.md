# 硬件资料分级

本目录按可信度隔离硬件资料，避免旧板引脚表再次被用于当前固件。

- `current/`：当前实物开发的首选依据。包含 V5.0 原理图及其 PDF 图面。
- `legacy/`：旧板或来源不一致的历史资料，只能用于追溯差异，禁止直接据此定义固件引脚。

发生冲突时按以下顺序判断：当前实物测试结果 > `current/` 原理图实际网络 > 当前官方程序 > `legacy/` 历史资料。
